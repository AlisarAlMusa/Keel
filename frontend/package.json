{
  "name": "keel-frontend",
  "private": true,
  "workspaces": [
    "ui",
    "widget",
    "admin",
    "portal"
  ],
  "scripts": {
    "build": "npm run build --workspace=ui && npm run build --workspace=widget && npm run build --workspace=admin && npm run build --workspace=portal",
    "build:ui": "npm run build --workspace=ui",
    "build:widget": "npm run build --workspace=widget",
    "build:admin": "npm run build --workspace=admin",
    "build:portal": "npm run build --workspace=portal",
    "dev:widget": "npm run dev --workspace=widget",
    "dev:admin": "npm run dev --workspace=admin",
    "dev:portal": "npm run dev --workspace=portal"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
