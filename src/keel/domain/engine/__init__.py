"""The deterministic engine (domain/engine/) — the crown jewel.

Pure, deterministic, no I/O, no LLM. dag / audit / validator / sections /
workload / planner land in Phase 1, with human-approved edge-case tests written
FIRST (constitution Principle II). Empty in Phase 0.
"""
